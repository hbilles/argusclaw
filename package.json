{
  "name": "argusclaw",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n gateway,bridge -c blue,green \"npm run dev -w packages/gateway\" \"npm run dev -w packages/bridge-telegram\"",
    "build": "npm run build --workspaces",
    "docker:up": "docker compose up --build"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "concurrently": "^9.0.0",
    "rimraf": "^6.0.0"
  }
}
