{
  "name": "argusclaw",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n gateway,bridge -c blue,green \"npm run dev -w packages/gateway\" \"npm run dev -w packages/bridge-telegram\"",
    "dev:slack": "concurrently -n gateway,slack -c blue,magenta \"npm run dev -w packages/gateway\" \"npm run dev -w packages/bridge-slack\"",
    "dev:all": "concurrently -n gateway,telegram,slack -c blue,green,magenta \"npm run dev -w packages/gateway\" \"npm run dev -w packages/bridge-telegram\" \"npm run dev -w packages/bridge-slack\"",
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present",
    "test:coverage": "vitest run --coverage",
    "docker:up": "docker compose up --build"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "concurrently": "^9.0.0",
    "rimraf": "^6.0.0",
    "typescript": "^5.7.0"
  }
}